import React from 'react';

function Event({ event_name, date_start, time_start, street_address, city, state, zipcode }) {
  return (
    <div>
      <h1>{first_name}</h1><h1>{date_start}</h1>
      <h1>{time_start}</h1>
      {street_address ? <h1>{street_address}</h1> : null}
      {profile_image ? <img alt={last_name} src={profile_image}></img> : null}
      <p>{street_address}</p>
      <button><a href='/alumni'  >Alumni Page</a></button>

    </div>
  );
}

export default Event;